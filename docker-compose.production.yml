# docker-compose.production.yml

volumes:
  pg_data_production:
  static_volume:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data_production:/var/lib/postgresql/data
    networks:
      - taski_network
    restart: always
  backend:
    image: korsarr/taski_backend
    env_file: .env
    volumes:
      - static_volume:/backend_static
    restart: always
    networks:
      - taski_network
  frontend:
    image: korsarr/taski_frontend
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static_volume:/frontend_static
    restart: always
    networks:
      - taski_network
  nginx:
    image: korsarr/taski_nginx
    env_file: .env
    volumes:
      - static_volume:/staticfiles/
    ports:
      - 8000:80
    restart: always
    networks:
      - taski_network

networks:
  taski_network:
    driver: bridge